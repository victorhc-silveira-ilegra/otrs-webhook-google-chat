#!/usr/bin/env bash
set -euo pipefail
ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/../../.." && pwd)"
PY="$(bash "${ROOT}/linters/git-hooks/bin/resolve_venv_python.sh")"
exec "${PY}" -m pre_commit "$@"
