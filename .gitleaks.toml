title = "gitleaks"

[allowlist]
paths = [
  '''(.*?)(\.|/)(example)$''',
  '''\.env\.example$''',
]

[[rules]]
id = "generic-api-key"
description = "Generic API Key"
regex = '''(?i)(api[_-]?key|secret|token|password)\s*[:=]\s*['\"]?[A-Za-z0-9/\+=]{16,}['\"]?'''
